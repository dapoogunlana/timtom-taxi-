import { EventcatcherDirective } from './eventcatcher.directive';

describe('EventcatcherDirective', () => {
  it('should create an instance', () => {
    const directive = new EventcatcherDirective();
    expect(directive).toBeTruthy();
  });
});
